# Search
# Serper (https://serper.dev) – Google search API
SERPER_API_KEY=your_serper_api_key

# LLM (set one; only Gemini is fine)
# Gemini (same key as in the REST API / curl examples)
GEMINI_MODEL=gemini-2.0-flash
GEMINI_API_KEY=your_gemini_api_key
# Model (default: gemini-1.5-flash). Free tier may have no quota for gemini-2.0-flash.
# Or OpenAI instead
OPENAI_API_KEY=your_openai_api_key

# Paths (optional; defaults work when run from repo root with volume mount)
# DEGREES_JSON=/data/src/data/degrees.json
# OUTPUT_DIR=./output